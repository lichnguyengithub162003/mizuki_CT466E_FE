# Laravel API prefix. Use a same-origin proxy or full backend URL in local development.
VITE_API_BASE_URL=/api/v1

# Laravel origin used when requesting the Sanctum CSRF cookie.
VITE_SANCTUM_BASE_URL=http://localhost:8000
